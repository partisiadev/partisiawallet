package config

type App struct {
}

const (
	// AppConfigPathDirName app directory (indexedDB, platformDB)
	AppConfigPathDirName = "partisiawallet"

	// AppWalletDBDirName database directory
	AppWalletDBDirName = "walletDB"
)
